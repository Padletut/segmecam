#!/usr/bin/env bash
set -euo pipefail
# Finn "ekte" bazel (tilpass hvis den ligger et annet sted)
REAL_BAZEL="${REAL_BAZEL:-/usr/bin/bazel-7.4.1}"

if [[ "${ALLOW_BAZEL:-}" != "1" ]]; then
  echo "🚫 Never call 'bazel' directly here. Use: ./build-app.sh"
  exit 1
fi

exec "$REAL_BAZEL" "$@"
