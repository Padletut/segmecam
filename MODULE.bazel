module(name = "segmecam")

# Bzlmod disabled - using WORKSPACE for all dependencies
# MediaPipe requires specific protobuf 5.28.3 version from WORKSPACE
# which conflicts with Bzlmod's automatic dependency resolution.
#
# Note: Assimp IS available in Bazel Central Registry (5.4.3.bcr.6)
# but we use WORKSPACE approach to avoid version conflicts.
